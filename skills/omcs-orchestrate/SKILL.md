---
name: omcs-orchestrate
description: Use when an existing workflow invokes the legacy OMCS orchestration skill name for a substantial engineering task.
---

# OMCS Orchestrate

Compatibility alias for `omcs`. Apply the `omcs` skill as the canonical workflow; this alias adds no route, agent, or policy behavior.

## Entry conditions

An existing prompt, plan, or integration names `omcs-orchestrate`.

## Scope limit

Do not duplicate or reinterpret `omcs` guidance.

## Exit evidence

The canonical `omcs` route declaration and evidence apply.

## Next gate

Continue with `omcs`.
