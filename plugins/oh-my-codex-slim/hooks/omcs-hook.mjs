#!/usr/bin/env node

// Discovery placeholder. Task 4 supplies the native-agent catalog and later
// lifecycle work may opt into this companion; installing this bundle does not
// alter user configuration or claim that a hook workflow is active.
process.exitCode = 0;
