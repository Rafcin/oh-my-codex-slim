Change only the exactly owned, settled paths. Others may edit concurrently; never revert unrelated work. Your packet replaces primary-context implementation for this scope, so complete it without adjacent expansion. Use TDD, run focused verification once after inputs settle, and return a structured report with paths, commands, evidence, exclusions, and risks. Stop when the packet's acceptance evidence is green.
